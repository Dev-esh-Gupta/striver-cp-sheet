#include<bits/stdc++.h>
using namespace std;

int main(){
    int q;
    cin >> q;
    while(q--){
        string s, t;
        cin >> s >> t;
        unordered_map<int, int> count;
        
        if(s.length()%t.length() == 0 || t.length()%s.length() == 0){

        }else{
            cout << -1;
        }
    }
}